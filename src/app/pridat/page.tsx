// src/app/pridat/page.tsx

import Typography from '@mui/material/Typography';

export const metadata = {title:" Pridanie prispevka| MojaAppl"};

export default function AddPost() {
    return (
        <Typography>Pridanie prispevka</Typography>
    );
}