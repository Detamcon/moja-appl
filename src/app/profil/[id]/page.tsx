// src/app/profil/[id]/page.tsx

import Typography from '@mui/material/Typography';

export const metadata = {title:" Detail profilu | MojaAppl"};

export default function ProfileDetail() {
    return (
        <Typography>Detail profilu</Typography>

    );
}